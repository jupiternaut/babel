import {
  TeamInboxFanIn,
  TeamInboxOrgClient,
  type TeamInboxMaterializedDelivery,
  type TeamInboxOrgDescriptor,
  type TeamInboxSnapshot,
} from '@nimbalyst/collab-bundle/inbox';

const descriptor = null as unknown as TeamInboxOrgDescriptor;
const delivery = null as unknown as TeamInboxMaterializedDelivery;
const snapshot = null as unknown as TeamInboxSnapshot;

void TeamInboxFanIn;
void TeamInboxOrgClient;
void descriptor;
void delivery;
void snapshot;
