import {
  FeedbackRequestSync,
  FeedbackRequestRespond,
  type FeedbackRequestRespondProps,
  type FeedbackRequestServiceState,
} from '@nimbalyst/collab-bundle/feedback-ui';

const component: React.FC<FeedbackRequestRespondProps> = FeedbackRequestRespond;
const state = null as unknown as FeedbackRequestServiceState;

void component;
void state;
void FeedbackRequestSync;
