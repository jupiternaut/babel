import type { TrackerCoreContext } from "@nimbalyst/tracker-core";
/** Runtime adapter only: core semantics receive the active registry per call. */
export declare const runtimeTrackerContext: TrackerCoreContext;
