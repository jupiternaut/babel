import { MediaViewerEditor } from './MediaViewerEditor';
import './styles/mediaViewer.css';

// Export the component for the extension system
export const components = {
  MediaViewerEditor,
};
