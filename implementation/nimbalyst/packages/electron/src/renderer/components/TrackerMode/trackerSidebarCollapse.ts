/** Re-export shim; sidebar collapse normalization is shared with the browser console. */
export {
  normalizeCollapsedOwnershipSections,
  normalizeExpandedNavFolders,
  toggleListEntry,
} from '@nimbalyst/collab-client/trackers';
