/** Re-export shim; the plan status-drift presentation is shared with the browser console. */
export {
  resolvePlanStatusDriftPresentation,
  shortSha,
} from '@nimbalyst/collab-client/trackers';
export type { PlanStatusDriftPresentation } from '@nimbalyst/collab-client/trackers';
