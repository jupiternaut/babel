# 巴别塔 TASK：开发与验收清单

更新：2026-09-16。盘点基线：`44683aa04c43d4a26eaefcf19c6554a0f9ed9640`。主开发与桌面验收环境：MacBook M3。开发会话和验收会话都从本文接续；需求以 [SPEC](NIMBALYST-DEVELOPMENT-SPEC.md)、[CAP](CAPABILITY-MATRIX.md)、[WD](WAYLAND-DEVIN-BENCHMARK.md) 为准。

## 两个标记分别打

| 开发完成 | 验收通过 | 含义 |
|---|---|---|
| `[ ]` | `[ ]` | 未开始、开发中或仍有实现缺口；在备注写清 |
| `[x]` | `[ ]` | 程序做好了，有实现与自测证据，等待验收会话复核 |
| `[x]` | `[x]` | 验收会话按本行条件实际验证通过，已登记版本、平台和证据 |

- **开发完成**：本行范围内的实际入口已接通，相关代码检查及行为测试通过；只有类型、模拟占位或禁用按钮不能打勾。验收会话可以检查代码后确认或撤销这个标记。
- **验收通过**：验收会话先确认代码，再按验收条件操作运行中的程序；正例、拒绝/失败路径和跨端结果都符合要求才打勾。仅看代码或绿色单测不能打勾。美观的最终认可另需用户实际反馈。
- 每次打勾附上 **任务 ID、代码提交、验收日期、平台、实际操作、结果与证据链接**。发现回归，取消受影响的标记并写原因，不删除历史证据。
- 一行只覆盖写明的范围。已通过的 demo 子路径不等于整项 CAP/M0/真实 Agent 完成；本轮自测不自动冒充另一验收会话的结论。
- 开始工作时核对 HEAD 和工作树，在“当前接续”登记负责切片；结束时更新本表和 [TASK_EVIDENCE](TASK_EVIDENCE.md)。不从旧 WB 清单重新开发已存在功能。

## 当前接续

当前切片：**M1-02a / Pi RPC 传输与隔离预检（工程前置）**。延续用户明确的分工：Pi 执行，Babel 绑定任务、展示输出、传递输入和验收。已实现协议传输及 CLI 空配置预检；真实 Pi 0.84.1 握手、读回空会话和进程退出已自测，未调用模型。完整检查及证据见文末对应记录；本片不等于三端 Pi 任务接通。

上一片 M0-05a 已开发完成：双向依赖/阻塞、核心守卫、原生与终端显式保存；独立验收仍留空。M0-16 的终端冲突选择与完整草稿/选区恢复仍在待办，不因开始 Pi 接入而标为完成。

M0-10a 的取消待确认/失联核对已开发完成，仍待独立验收；下方操作单继续有效。

当前优先路线：最小 Pi 会话绑定与输入/输出 → 真实运行状态与结果审查 → 相应 GUI/TUI/CLI 实操；M0 编辑、视图、恢复等缺口继续逐片收口，不再把所有 M0 打磨作为开发最小 Pi 适配的前提。真实模型调用仍须明确 provider/model/账号范围，不能自动使用已有凭据。每片同步处理相关 UI/UX；本轮无凭据工程预检不替代产品功能验收。

## 已有成果：仅这些子路径已具备历史验收证据

以下验收标记依据已提交的 Mac 实测回填；不表示本轮重新运行，也不外推到右侧范围之外。代码与证据版本为 `44683aa`。

| ID | 已交付的具体子功能 | 开发完成 | 验收通过 | 证据 / 边界 |
|---|---|---|---|---|
| BASE-01 | Mac 隔离开发启动器 start/status/stop | [x] | [x] | [启动与环境证据](TASK_EVIDENCE.md)；不是发布安装包 |
| BASE-02 | 关注四状态、排除归档、原记录定位与 CLI/TUI 过滤 | [x] | [x] | [关注证据](implementation/evidence/macos-20260916/attention-evidence.json)；完整组合/断线仍见 M0-01/17 |
| BASE-03 | 原生标题显式保存、远端冲突不覆盖、切任务保留草稿 | [x] | [x] | [标题证据](implementation/evidence/macos-20260916/title-evidence.json)；不含全部正文/字段 |
| BASE-04 | GUI 中文创建→CLI 同 ID→真实 PTY 启动/验收，关闭 GUI 后服务继续 | [x] | [x] | [跨端证据](implementation/evidence/macos-20260916/three-surfaces-identity.json)；模拟执行 |
| BASE-05 | 原生归档/恢复保持身份，不自动重跑 | [x] | [x] | [原生验收索引](implementation/evidence/macos-20260916/README.md)；不含所有批量/右键/PTY 分支 |
| BASE-06 | demo 服务 executing/verifying 强杀后，同 run 恢复至待验收 | [x] | [x] | [进程恢复记录](TASK_EVIDENCE.md)；不等于生产 Worker 接管 |
| BASE-07 | 默认 CSS 磨砂、宿主浅深主题、局部 SVG 可选实验及媒体模拟回退 | [x] | [ ] | [界面与自测](TASK_EVIDENCE.md)；待完整视觉、系统设置和性能验收，见 UX-01～03 |

## M0：需要补齐的功能

实现细节及已有部分测试见 [M0-CAP-STATUS](implementation/M0-CAP-STATUS.md)。这里的空开发标记表示本行剩余范围尚未全部做好，不表示该 CAP 完全没有代码。

| ID / 对应 CAP | 要开发的功能 | 开发完成 | 验收通过 | 验收会话检查什么 |
|---|---|---|---|---|
| M0-00 / NB01 | 当前 v2.3 OpenAPI、事件/Hook Schema、兼容迁移与契约检查 | [ ] | [ ] | 机器合同与实际接口正反例一致；旧 reference 合同不算已迁移；作为 M1-01 前置 |
| M0-01 / 01 | 项目、设备、类型、搜索、状态、关注组合筛选 | [ ] | [ ] | GUI/真实 PTY/CLI 同一筛选下 ID 与计数一致，清除/返回不串作用域 |
| M0-02 / 02 | 原生类型、Ready、Saved Views 的保存/应用/编辑/取消共享 | [ ] | [ ] | 原生保存→PTY 应用→CLI 读回同一定义；保留原类型语义 |
| M0-03 / 03 | 当前类型与自定义字段的新建，取消与防重提交 | [ ] | [ ] | 中文与字段完整读回；取消不创建；连按/超时不生成两条 |
| M0-04 / 04 | 正文/字段编辑和内容 revision 路由 | [ ] | [ ] | GUI 改正文/字段→PTY/CLI 读回；富文本不丢，旧版本提交被拒 |
| M0-04a / 04、16 | 原生正文显式保存、冲突选择、会话草稿与三端版本保护 | [x] | [ ] | [开发自测证据](implementation/evidence/m0-04a-20260916/validation.json)；按下方正文操作单独立复验；字段、完整选区/断线恢复仍属 M0-04/16 |
| M0-04b / 04、05、16 | 优先级、负责人、标签的三端编辑、显式保存与版本保护 | [x] | [ ] | [开发自测证据](implementation/evidence/m0-04b-20260916/validation.json)；按下方字段操作单复验；自定义字段、类型、关系及完整选区/断线恢复不在本行范围 |
| M0-05 / 05 | 双向关系、依赖、优先级的完整三端入口 | [ ] | [ ] | 双向关系一致；循环/只读/冲突拒绝不改数据 |
| M0-05a / 05、16、19 | 双向依赖/阻塞编辑、关系守卫、冲突保稿与反向端点同步 | [x] | [ ] | [开发自测证据](implementation/evidence/m0-05a-20260916/validation.json)；按下方关系操作单独立复验；任意关系类型、创建时带依赖及完整冲突/选区恢复仍待后续 |
| M0-06 / 06 | 共享手工排序，菜单/键盘替代 | [ ] | [ ] | 三端 orderKey 一致，不修改正文；M1 再验必需拖拽路径 |
| M0-07 / 07 | 完整启动摘要、最新计划/版本与显式确认 | [ ] | [ ] | 确认前不启动；连按/丢响应仅一个 run；accepted 不冒充完成 |
| M0-08 / 08 | 独立工具事件轨迹、会话进度与长日志 | [ ] | [ ] | tool.started/finished 顺序与同 run 一致；看旧消息不被抢滚动 |
| M0-09 / 09 | 消息/请求回答跨端完整输入与防重复 | [ ] | [ ] | 按 requestId 回答；重复拒绝；在途切任务不串写、不丢草稿 |
| M0-10a / 10 | GUI/TUI 的失联和取消待确认核对入口、确认与共享写守卫 | [x] | [ ] | [本轮自测证据](implementation/evidence/m0-10a-20260916/validation.json)；按下方步骤独立复验；仅 demo 核对，不声称终止真实 Worker |
| M0-10b / 10 | 完整取消→终止确认→允许重试/归档跨端闭环 | [ ] | [ ] | 未确认始终禁止重试/归档；确认后才允许；所有原生/终端入口一致 |
| M0-11 / 11 | 按 run 浏览差异/产物，TUI/CLI 导出与文件权限/版本校验 | [ ] | [ ] | GUI/TUI/CLI 文件、基线、ID 一致；不可用产物明确拒绝；真实 Git 见 M1-03 |
| M0-12 / 12 | 可编辑修改要求、完整验收/豁免/身份策略 | [ ] | [ ] | 缺必需证据不能完成；Hook 不自批；TUI 可输入修改说明 |
| M0-13 / 13 | TUI 可发现重试入口与新执行摘要 | [ ] | [ ] | 新 attempt/runId，旧证据保留；未终止禁止重试 |
| M0-14 / 14 | 全部右键/批量与真 PTY 归档/恢复 | [ ] | [ ] | 保持 trackerId 与历史；恢复不启动；未终止拒绝 |
| M0-15 / 15 | TUI 旧 run 选择/详情/讨论，跨端历史只读 | [ ] | [ ] | 讨论不冒充运行消息；看旧 run 不改变当前执行或写到旧 run |
| M0-16 / 16 | 正文/字段/选区草稿及跨端冲突解决 | [ ] | [ ] | 双端制造冲突，中文草稿/选区不丢，可选择采用远端或重新提交 |
| M0-17 / 17 | 显式重连与 GUI/PTY 断线恢复 | [ ] | [ ] | 游标重复/过期、服务重启后同 run，无消息洞；选择/草稿/焦点/终端模式恢复 |
| M0-18 / 18 | GUI/TUI 开发场景注入与 demo 重置入口 | [ ] | [ ] | 只影响指定 demo profile；不访问真实凭据/设备或误重置用户数据 |
| M0-19 / 19 | 所有原生正文/批量/拖拽/右键统一能力与拒绝原因 | [ ] | [ ] | 同权限得到相同结果；未适配入口不回退真实 IPC；生产 MCP 见 M1 |
| M0-20 / 20 | 挂载原生 Hook 配置/投递管理及三端操作 | [ ] | [ ] | 校验拒绝/超时不提交；观察重试只投递、不重跑业务；真实输入验证 |

### M0-10a 给验收会话的操作单

1. 使用隔离 demo 和同一提交的 Mac GUI、真实 PTY、CLI；分别准备 lost 与 cancel_requested 执行，记录 projectId/trackerId/runId/revision。
2. 在 GUI 打开“核对”确认，按取消/Escape；状态、revision 和业务事件不变。TUI 菜单重复此检查。
3. 明确确认 demo 核对结果后，CLI 查询同一个 run；结果为 cancelled 或 failed，不是 succeeded/DONE，不新增 run；关联 run.finished 事件只能提交一次。
4. 确认页打开后切任务/项目或查看旧 run，不能把确认发到另一个目标；连按不能重复提交。
5. 只读条目、过期 revision、状态已改变或权限不足时拒绝，不能修改记录或生成终止事件；界面显示原因并允许重新查询。
6. 核对前重试/归档被共享守卫拒绝；核对后显式重试生成新 run，旧 run 和历史保留。
7. 检查浅深主题、Tab/Enter/Escape、焦点、中文提示与窄窗口。把截图、PTY 记录、查询和事件结果写入证据，再决定是否勾验收列。

### M0-04a 给验收会话的操作单

1. 同一提交与隔离 demo 中打开原生详情正文；输入中文、多段落、标题、列表、链接与代码块。点击保存前 CLI 查询仍为旧正文；显式保存后 CLI/TUI 读取同一 trackerId 的正文与新 revision。
2. 留下未保存正文，切换任务再返回；草稿保留且不自动写入。关闭/重新挂载详情也保留当前会话草稿；退出应用后的持久草稿不在本片范围内。
3. 由 CLI 更新同记录制造版本冲突；GUI 不覆盖中文草稿，保存按钮禁用，展示远端正文。分别操作“采用远端正文”与“继续编辑草稿”，后者仍需再次明确保存才写入。
4. 检查只读记录、无有效版本、网络/服务拒绝与连续点击；拒绝不得写入，错误可见，草稿保留，不能串写另一任务/项目。
5. 真实 PTY 的 e 编辑正文，Tab 切字段，Ctrl+S 保存；制造旧版本拒绝后原编辑浮层和正文保留。后台筛选结果变化时仅对原 trackerId 尝试保存；重复 Ctrl+S 只发一次。Escape 明确取消并丢弃本次终端草稿；终端冲突选择与跨关闭恢复仍待 M0-16。
6. 实际检查主题、键盘、中文组合输入、聚焦/展开正文、窄窗口和富文本往返；记录版本、截图、PTY、查询与事件证据后再决定验收标记。本轮开发者自测不代替独立会话验收。

### M0-04b 给验收会话的操作单

1. 同一提交与隔离 demo 中，在原生详情修改优先级、中文负责人、标签；明确保存前 CLI 查询仍是旧值，保存后 GUI/TUI/CLI 同 trackerId、字段与 revision 一致，不启动 run。
2. 留下字段草稿，切任务再返回，不能串写或丢失；明确撤销不写入。空负责人和空标签保存后从三端读回为空。
3. 真实 PTY 按大写 `F`（小写 f 仍为 PDF），Tab 切字段，Ctrl+S 保存。标签以英文或中文逗号分隔；连续 Ctrl+S 仅一次提交。Escape 取消并丢弃本次终端草稿，退出恢复终端模式。
4. GUI 留草稿，再从 CLI 修改同记录，确认 GUI 展示远端字段、保留中文草稿并禁用旧版本保存。“继续编辑字段草稿”只重定基线，须再次保存；仅覆盖已修改字段。另测“采用远端字段”。
5. 只读、缺失/非法/过期版本、非法字段类型及服务拒绝均不得写入。TUI 旧版本提交保留浮层与输入；后台选择变化不能将保存发给另一任务。适配器批量格式预检不等于跨记录原子事务。
6. 检查浅深主题、窄窗、键盘焦点、系统中文输入法与 VoiceOver；记录提交、profile、截图、PTY、查询和事件结果后再勾验收。本片不包含自定义字段/关系/类型编辑，也不包含退出应用后的草稿持久化。

### M0-05a 给验收会话的操作单

1. 同一提交与隔离 demo 创建甲、乙、丙。在原生“Depends On”选择乙，保存前 CLI 仍为空；显式保存后甲.dependsOn 包含乙，乙.blocks 包含甲，两端 revision 与 task.updated 事件各更新一次。
2. 在“Blocks”增删关联，或用真实 PTY 的 `l` → Tab → Ctrl+S 编辑；从相反方向删除后两端同时消失，GUI/CLI 同步刷新。未变关系不递增版本；重复幂等键不重复业务事件。
3. GUI 留下未保存关系，再由 CLI 改同记录制造冲突；草稿保留并显示远端标题/ID，可采用远端或明确重定版本后再次保存。切任务再返回不丢草稿，不能串写另一目标。
4. 甲依赖乙、乙依赖丙后，尝试丙依赖甲；循环、自关联、跨项目/不存在目标、过期/缺失版本必须拒绝。修改任一只读端点的正向或反向边都拒绝，所有记录、版本、事件和 outbox 保持不变。
5. CLI `relation set` 携带 `--expected-revision`；可用 JSON 空数组清除。创建命令不得直接注入非空依赖/阻塞，普通字段更新不得改关系；应明确指引先建记录再调用 relation.set。
6. PTY 拒绝后保留原目标、revision、表单和错误；双 Ctrl+S 只提交一次，Esc 取消不写入，q 退出恢复终端。原生关系输入有可读标签，Escape/Cancel 关闭添加框后回到 Add link。
7. 检查浅深主题、窄窗、键盘、中文输入法和 VoiceOver，记录提交/profile/截图/PTY/双端查询与事件后再勾验收。此行只覆盖 dependsOn/blocks，不代表全部关系词汇、创建时关系编排或完整 M0-05/16 完成。

## UI/UX 与平台验收

| ID | 要完成的功能或验收 | 开发完成 | 验收通过 | 通过条件 |
|---|---|---|---|---|
| UX-01 | V01～V05 完整视图与异常状态，玻璃/正文分层 | [ ] | [ ] | 真 Mac 窗口逐页实际操作，空/加载/错误/冲突/只读可理解；用户审美确认另记 |
| UX-02 | 窄屏、200% 字体、焦点、中文组合输入、VoiceOver | [ ] | [ ] | 按 SPEC 宽度检查点；全部主要动作可达；关浮层焦点恢复；文字不裁切 |
| UX-03 | 真系统偏好回退、合成背景对比度与性能 | [ ] | [ ] | 减少透明度/动效即时生效；CSS/折射同环境测量；500 卡/10000 消息；未过预算保持 CSS |
| SYS-01 | Mac 原生“设备与服务”采样、隔离进程操作与权限错误 | [ ] | [ ] | 按 [SYS-01～09](SYSTEM-CONSOLE-SPEC.md) 实机 GUI/PTY/CLI 复验；底层测试不代替界面验收 |
| SYS-02 | macOS launchd 服务/自启动适配与回读 | [ ] | [ ] | 仅隔离目标，启停/自启动独立、失败可解释，PID 身份和恢复正确 |
| SYS-03 | Windows / Ubuntu 新版本回归 | [ ] | [ ] | 分平台固定提交和实机证据；不以 Windows 历史结果或 Mac 测试代替 |

## M1 与 WD 的真实执行能力

| ID / 对应要求 | 要开发的功能 | 开发完成 | 验收通过 | 通过条件 |
|---|---|---|---|---|
| M1-01 / NB05～06 | 生产 Gateway/认证/SQLite/outbox/SSE，生产适配与 MCP 写守卫 | [ ] | [ ] | 多客户端、迁移/备份/恢复、权限与 Hook 无绕过，生产部署证据 |
| M1-02 / NB07～08、WD04/05/08 | 真实 Pi Worker 与三端全生命周期 | [ ] | [ ] | 明确授权的 provider/model/账号；真实代码任务、同 run/session、退出不双执行、人工验收再 DONE |
| M1-02a / 工程前置 | Pi RPC 传输与 CLI 隔离预检（不含三端任务功能） | [x] | [ ] | [合同与入口](implementation/nimbalyst/packages/babel/PI-INTEGRATION.md)、[自测证据](implementation/evidence/pi-rpc-20260916/validation.json)；明确指定 Pi，空配置仅 get_state/get_messages；超时不重试，确认预检进程退出，不标模型执行完成 |
| M1-03 / WD07 | 真实 Git 基线/diff、测试证据与产物交付 | [ ] | [ ] | 文件内容/版本/hash 可核实；真实修改与验收对应；不是模拟 patch |
| M1-04 / WD03 | 产品内多 Agent 团队、分工、依赖、文件拥有者和 worktree 隔离 | [ ] | [ ] | 可观察的真实团队运行；共享文件串行；明确模型权限；开发会话的子 Agent 不算产品交付 |
| M1-05 / WD06 | 暂停→确认暂停→人工接管→恢复 | [ ] | [ ] | 控制权确认前不发生冲突写；恢复同任务且不产生第二个活跃 run |
| M1-06 / UI/FLOW | GUI 拖拽与菜单/键盘业务等价 | [ ] | [ ] | 合法动作走共享命令，不合法列拒绝；不能拖成假成功 |

## M2 / M3 与最终发布

| ID | 要开发的功能 | 开发完成 | 验收通过 | 通过条件 |
|---|---|---|---|---|
| M2-01 / NB09、WD09 | 真实 SSH 设备与三平台节点快照/失联核对 | [ ] | [ ] | 可达/Worker 健康分开，60 秒新鲜度和过期明确，不误控制另一设备 |
| M2-02 / NB10 | Google OAuth 选表/导入/同步/冲突/撤权 | [ ] | [ ] | 授权测试账号真实 API；分页/重复/外部删除不丢任务、不误标执行成功 |
| M2-03 / NB11 | 文件、聊天、运维引用创建待办 | [ ] | [ ] | 引用可追溯，只创建待办，不自动执行 |
| M2-04 / WD10 | Pi/Codex 等厂商 Hook 的真实适配 | [ ] | [ ] | 明确授权环境下厂商事件→公共合同；重复/超时不重复业务，不自动人工验收 |
| M3-01 / NB12 | PDF 阅读、定位、对译、批注/导出 | [ ] | [ ] | 真实 PDF 样本与翻译结果，版本/段落映射可追溯；fixture 锚点不是成品 |
| M3-02 / NB12 | iOS/iPadOS/Android 等客户端与六平台验收 | [ ] | [ ] | 按实际平台独立操作与证据，不把窄桌面窗口算移动 App |
| REL-01 | Mac 本地安装包、安装升级与数据保留 | [ ] | [ ] | 从包安装启动，升级/退出/恢复与权限实际操作 |
| REL-02 | 签名、公证、平台发布与整体验收 | [ ] | [ ] | 发布门禁、用户视觉签收及必要平台回归齐全；发布按用户授权执行 |

## 验收记录怎么追加

```text
任务 ID：
开发提交：
验收提交（若不同，说明差异）：
日期 / 平台 / 独立 profile：
验收会话或负责人：
实操与结果：
证据链接（截图、PTY、查询/事件、日志）：
结论：通过 / 不通过 / 部分通过（部分通过不勾整行）
未覆盖或回归：
```

当前基线验收索引：[Mac 证据](implementation/evidence/macos-20260916/README.md)、[TASK_EVIDENCE](TASK_EVIDENCE.md)。后续只追加实际发生的结果。历史 WB 清单见 [旧任务说明](reference/pre-v2.3-repository/TASKS.md)，不直接派发；并行文件归属仍按 [MULTI-AGENT-PLAN](MULTI-AGENT-PLAN.md)。
