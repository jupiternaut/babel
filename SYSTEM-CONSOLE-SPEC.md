# 巴别塔 × Nimbalyst：真实设备与服务控制

用户已授权直接实现可用产品能力。本模块不使用 M0 假设备数据，不替换原生 Trackers。实施代码与操作说明在 [SYSTEM-CONSOLE.md](implementation/nimbalyst/packages/babel/SYSTEM-CONSOLE.md)。

## 界面和职责

左侧增加独立“设备与服务”入口。中央只展示当前服务或进程列表；顶部为 CPU、内存、磁盘容量摘要；详情显示日志和身份。原生宿主保持主题、Trackers、任务看板及 Agent 会话入口。服务运行按钮与自启动开关分别显示真实状态、禁用原因和错误。

GUI / TUI / CLI → 公共控制合同 → 独立后台 → OS 适配器。GUI 不直接调用 PowerShell；TUI 不要求打开 Electron。客户端断开不取消已被系统接受的操作，客户端重连可按 requestId 找回。

## 验收要求

| 编号 | 能力 | 完成证据 |
|---|---|---|
| SYS-01 | CPU/内存/磁盘/进程真实采样 | 本机采样，过期时间明确；权限缺失不伪造 0 |
| SYS-02 | 登记服务启停重启 | 隔离真实进程生命周期、反向依赖保护、状态回读 |
| SYS-03 | 自启动独立配置 | OS 触发器回读；未知/不支持/权限失败明确 |
| SYS-04 | 进程终止 | PID+startedAt 身份及确认，PID 复用拒绝 |
| SYS-05 | 原生宿主 | 独立入口、原生布局保留、真实窗口鼠标/键盘/窄屏验收 |
| SYS-06 | 无头终端 | 真正 ConPTY 输入/鼠标/resize/中文；退出界面后后台存活 |
| SYS-07 | 持久操作 | 请求幂等、冲突拒绝、异常中断不重放、事件续读 |
| SYS-08 | Hooks | 前置拒绝/超时、观察游标/重试/重复投递去重语义 |
| SYS-09 | 权限和发布 | 回环鉴权、IPC 来源、profile ACL、UAC 取消、全项目门禁 |

本轮目标机器是 Windows/WSL；真实 GitLab、DUFS、局域网代理维持用户既定停用状态。需要写操作的验收使用隔离目标，不用重新开启这些服务来证明按钮有效。

完整产品路线仍包括远程设备、权限管理、实时磁盘 I/O 与更完整服务发现。这些不能被当前本机能力的“已实现”覆盖。验收报告必须区分实现、自动化测试、实机操作、权限阻塞和未验证。
